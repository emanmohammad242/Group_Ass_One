<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.groupassigment1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GroupAssigment1">
        <activity android:name=".Activities.InsertBookActivity"></activity>
        <activity android:name=".Activities.EditBookActivity" />
        <activity android:name=".Activities.DetailesBookActivity" />
        <activity android:name=".Activities.InsertCategoryActivity" />
        <activity android:name=".Activities.BookListActivity" />
        <activity android:name=".Activities.CategoryListActivity" />
        <activity android:name=".Activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>